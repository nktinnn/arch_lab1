apiVersion: v1
kind: ConfigMap
metadata:
  name: helpdesk-config
  namespace: helpdesk-system
data:
  DB_HOST: dev-postgresql.infra.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: helpdesk
  SERVER_PORT: "8080"
